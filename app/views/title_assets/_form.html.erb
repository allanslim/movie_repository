<%= form_for(@title_asset) do |f| %>
  <% if @title_asset.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@title_asset.errors.count, "error") %> prohibited this title_asset from being saved:</h2>

      <ul>
      <% @title_asset.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :asset_id %><br>
    <%= f.number_field :asset_id %>
  </div>
  <div class="field">
    <%= f.label :title_id %><br>
    <%= f.number_field :title_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
