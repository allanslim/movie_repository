<%= form_for(@studios_title) do |f| %>
  <% if @studios_title.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@studios_title.errors.count, "error") %> prohibited this studios_title from being saved:</h2>

      <ul>
      <% @studios_title.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title_id %><br>
    <%= f.number_field :title_id %>
  </div>
  <div class="field">
    <%= f.label :studio_id %><br>
    <%= f.number_field :studio_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
