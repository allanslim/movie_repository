<%= form_for(@title_owner) do |f| %>
  <% if @title_owner.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@title_owner.errors.count, "error") %> prohibited this title_owner from being saved:</h2>

      <ul>
      <% @title_owner.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title_id %><br>
    <%= f.number_field :title_id %>
  </div>
  <div class="field">
    <%= f.label :studio_id %><br>
    <%= f.number_field :studio_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
