<div id="dashboard_bg" class="dashboard_bg"/>
  <br/><br/>
  <div class="container" >
    <div class="well" >

  <nav class="navbar navbar-default" role="navigation">
    <div class="container-fluid" >
      <ul class="nav navbar-nav">
        <li ><%= link_to 'Title', titles_path %></li>
        <li ><%= link_to 'Actors', actors_path %></li>
        <li class="active"><%= link_to 'Assets', assets_path %></li>
        <li><%= link_to 'Studios', studios_path %></li>
        <li><%= link_to 'Users', users_path %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to 'Logout',  '/logout' %></li>
      </ul>
    </div>
  </nav>    

      <h1>Listing assets</h1>

      <table class="table">
        <thead>
          <tr>
            <th>Assetvalue</th>
            <th>Assettype</th>
            <th colspan="3"></th>
          </tr>
        </thead>

        <tbody>
          <% @assets.each do |asset| %>
            <tr>
              <td><%= asset.assetValue %></td>
              <td><%= asset.assetType %></td>
              <td><%= link_to 'Show', asset %></td>
              <% if @isAdmin == 1 %>
                <td><%= link_to 'Edit', edit_asset_path(asset) %></td>
                <td><%= link_to 'Destroy', asset, method: :delete, data: { confirm: 'Are you sure?' } %></td>
              <% end %>
            </tr>
          <% end %>
        </tbody>
      </table>

      <br>
  <% if @isAdmin == 1 %>
      <%= link_to 'New Asset', new_asset_path %>
  <% end %>
    </div>
  </div>
</div>
