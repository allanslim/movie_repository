<%= form_for(@title_actor) do |f| %>
  <% if @title_actor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@title_actor.errors.count, "error") %> prohibited this title_actor from being saved:</h2>

      <ul>
      <% @title_actor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title_id %><br>
    <%= f.number_field :title_id %>
  </div>
  <div class="field">
    <%= f.label :actor_id %><br>
    <%= f.number_field :actor_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
